import Useprojetos from '@/components/hooks/projetos2'
export default function projetosPage(){
  // Useprojetos(() => {
  //   let url = window.location.origin + window.location.pathname
  // }, []
  return (
    <div>
      <Useprojetos />
    </div>
  )
}